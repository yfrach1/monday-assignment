import React from "react";

const SkipQuestion = ({ onClickskipQuestion }) => {
  return (
    <button className="generic_button" onClick={onClickskipQuestion}>
      skip question
    </button>
  );
};
export default SkipQuestion;
